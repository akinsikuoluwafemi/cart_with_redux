import {FETCH_PRODUCTS } from '../actions/types';

const initi


const productReducer = (state, action) => {
    switch(action.type){
        case FETCH_PRODUCTS:

            
        default:
            return state;
    }
}