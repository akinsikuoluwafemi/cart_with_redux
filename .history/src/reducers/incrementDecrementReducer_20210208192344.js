import { INCREMENT, DECREMENT } from '../actions/types';
import initialState from './initialState';





const incrementDecrementReducer = (state=init, action) => {
    switch(action.type){
        
    }


}

export default incrementDecrementReducer;